# agent 运行时镜像的版本钉点（spec: docs/specs/260909-agent-sandbox 需求 3）。
# 每个值都是 Dockerfile 的 ARG；构建时逐个 `--version` 校验，并写进镜像标签
# octagon.agent.<name>.version。改任何一行都意味着一个新的被测环境，要发新 tag。
BASE_IMAGE=ubuntu:24.04
NODE_MAJOR=22
# claude-code：官方 install.sh 指定版本（原生二进制）。2026-09-09 评测机实测版本。
CLAUDE_VERSION=2.1.270
# codex：GitHub release rust-v<ver> 的 musl 静态二进制。评测机实测版本。
CODEX_VERSION=0.154.0
# kimi-code：pip 包 kimi-cli。docs/agents.md 实测契约基于 0.29.1，该版本已不在
# PyPI；钉当前 latest，首次进沙盒时需按 docs/agents.md 重新核对事件流契约。
KIMI_VERSION=1.50.0
# opencode：npm 包 opencode-ai。docs/agents.md 实测版本。
OPENCODE_VERSION=1.18.30
# mimo-code：**不可精确钉版本**。npm 包 @mimo-ai/cli 无 0.1.9（ETARGET），
# 小米官方渠道是 curl https://mimo.xiaomi.com/install（Harbor 同款），不接收
# 版本参数。此值仅供 Dockerfile 的 test -n 守卫与镜像标签记录；实际版本以
# 装完 `mimo --version` 为准（smoke.sh 对 mimo 只验可运行、不比对钉点）。
MIMO_VERSION=unpinned
# dsh runtime：pip 包 deepseek-harness-runtime-bin，与 pyproject dsh extra 的
# deepseek-harness-sdk 同版本，否则 stdio JSON-RPC 协议可能不匹配。
DSH_VERSION=0.1.0rc6
