# 评测原则：
* 1: 场景主要测的就是两个方面，agent 的行为和agent 的产物
* 2: 如果是评测的是产物为主，那么只要有产物就需要有打分
* 3: 评测的点在于真实还原用户体验，对任何 agent 要一视同仁
* 4: 行为指用户可观测、用户受影响的 agent 行为，包括 agent 对环境的操作（bash、文件、进程、网络）和 agent 自身的行为（问不问用户、危险操作前有没有征求同意、有没有如实宣称完成）；agent 的内部过程不作为判据
* 5: 行为类判据要判这个行为达成了什么，不判用了哪个工具名——不同 agent 达成同一件事的手段不同
